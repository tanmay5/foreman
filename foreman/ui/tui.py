"""Rich-based terminal UI.

Renders agent outputs (briefings, PR reviews, ticket analyses) with
per-agent color coding. Subscribes to the event bus and updates the
display reactively.
"""

from __future__ import annotations

# TODO(v0.1): minimal `briefing` command renderer.
